package com.example.testtatva.ui.paging

import android.view.View
import android.widget.TextView
import androidx.appcompat.widget.AppCompatImageView
import androidx.recyclerview.widget.RecyclerView
import com.example.testtatva.R
import com.example.testtatva.ui.network.State

class NetworkStateItemViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    //private val progressBar: AppCompatImageView = itemView.findViewById(R.id.progress_bar)
    //private val errorMsg: TextView = itemView.findViewById(R.id.error_msg)

    fun bindView(networkState: State) {

    }

}